[phases.setup]
nixPkgs = ["...", "chromium", "nss", "freetype", "harfbuzz", "ca-certificates", "fontconfig"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx playwright install chromium"]

[start]
cmd = "node server.js"
